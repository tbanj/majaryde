import { SafeAreaView } from "react-native-safe-area-context";
import HomeComponent from "@/components/hoc/HomeComponent";

export default function Page() {
  return (
    <SafeAreaView>
      <HomeComponent />
    </SafeAreaView>
  );
}
